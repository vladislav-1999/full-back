services:
    - type: web
      name: todo-api-staging
      runtime: image
      region: frankfurt
      plan: free

      image:
          url: ghcr.io/vladislav-1999/full-back:dev
          creds:
              fromRegistryCreds:
                  name: ghcr-vladislav
      healthCheckPath: /ready

      envVars:
          - key: DATABASE_URL
            fromDatabase:
                name: todo-db-staging
                property: connectionString
          - key: JWT_SECRET
            sync: false
          - key: NODE_ENV
            value: production
          - key: LOG_LEVEL
            value: info
          - key: CORS_ORIGIN
            value: http://localhost:3000
databases:
    - name: todo-db-staging
      region: frankfurt
      plan: free
